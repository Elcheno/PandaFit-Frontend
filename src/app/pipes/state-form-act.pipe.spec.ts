import { StateFormActPipe } from './state-form-act.pipe';

describe('StateFormActPipe', () => {
  it('create an instance', () => {
    const pipe = new StateFormActPipe();
    expect(pipe).toBeTruthy();
  });
});
