import { FormatUUIDPipe } from './format-uuid.pipe';

describe('FormatUUIDPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatUUIDPipe();
    expect(pipe).toBeTruthy();
  });
});
